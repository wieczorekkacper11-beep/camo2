# dependencies
/node_modules
/.pnp
.pnp.js

# next.js build output
/.next/
/out/

# environment variables (NEVER commit secrets!)
.env
.env.local
.env.development
.env.production

# uploaded files on disk (now stored in DB as base64 - not needed)
/public/uploads/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel
